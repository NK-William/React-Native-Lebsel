import React from "react";
import { StyleSheet, Text, View } from "react-native";

const PromotersScreen = () => {
  return (
    <View>
      <Text style={{ justifyContent: "center", alignItems: "center" }}>
        promoters
      </Text>
    </View>
  );
};

export default PromotersScreen;

const styles = StyleSheet.create({});
