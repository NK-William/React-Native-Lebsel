import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SplashScreen = () => {
  return (
    <View>
      <Text
        style={{ color: "red", alignItems: "center", justifyContent: "center" }}
      >
        Loading
      </Text>
    </View>
  );
};

export default SplashScreen;

const styles = StyleSheet.create({});
